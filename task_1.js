const object1 = {
    size: 12,
    weight: 8,
};

function getInfo(object1){
    for ( let key in object1){
        if (object1.hasOwnProperty(prop)) {
            console.log(prop); 
        }   
    }
 }

getInfo(object1)












